export const ModelName = "InterfaceResilience";

# {ModelName}

The site compiler is configured for dollar-delimited math: $V(x) \leq V_{\max}$.

<aside>JSX-adjacent expressions remain under the MDX compiler contract.</aside>
