# Non-mathematical controls

The field kit costs \$125 and the annual service contract costs \$2,400.

The literal command `python -c "print('\\(not math\\)')"` must not be rewritten.

```python
legacy_example = r"\[not a rendered equation\]"
```
